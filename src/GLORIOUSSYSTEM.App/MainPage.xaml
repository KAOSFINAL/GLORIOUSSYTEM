<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GLORIOUSSYSTEM.App.MainPage">
    <Grid RowDefinitions="Auto,*" Margin="20">
        <Button Grid.Row="0" Text="Refresh" Clicked="OnRefreshClicked" HorizontalOptions="End" Margin="0,0,0,10"/>

        <CollectionView Grid.Row="1" x:Name="SensorList">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Stroke="LightGray" StrokeThickness="1" Padding="12" Margin="0,4">
                        <StackLayout Spacing="4">
                            <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold"/>
                            <Label Text="{Binding Type}" FontSize="12" TextColor="Gray"/>
                            <Label Text="{Binding LatestValueText}" FontSize="16" TextColor="DarkGreen" FontAttributes="Bold"/>
                        </StackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>